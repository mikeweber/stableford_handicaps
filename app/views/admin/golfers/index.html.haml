- content_for :header do
  Manage golfers

.col-md-3
  = link_to new_admin_golfer_path, class: 'btn btn-primary' do
    %span.glyphicon-plus
    Add golfer

%table.table.table-striped.table-hover
  %thead
    %tr
      %th Golfer
      %th Member ID
      %th Handicap
      %th Recent round
      %th Manage
  %tbody
    - @golfers.each do |golfer|
      %tr
        %td= link_to golfer.full_name, admin_golfer_path(golfer)
        %td= golfer.identifier
        %td= golfer.handicap
        %td= fdate(golfer.date_of_most_recent_round) || '(n/a)'
        %td
          = link_to edit_admin_golfer_path(golfer) do
            Edit
            %span.glyphicon.glyphicon-edit
          = link_to admin_golfer_path(golfer), data: { method: :delete, confirm: 'Permanently remove this golfer?' } do
            Delete
            %span.glyphicon.glyphicon-remove
